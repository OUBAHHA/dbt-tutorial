version: 2

models:
  - name: stg_orders
    descriptions: one unique order per row
    columns:
      - name: orderid
        description: the primary key for stg_orders
    #   - name: shipmode
    #     descriptions: "{{doc('shipmode')}}"